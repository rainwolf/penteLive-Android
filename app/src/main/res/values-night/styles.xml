<resources>

    <!-- Dark mode: white text (from colorPrimary) gets a black halo so it reads
         on varied backgrounds. Applied app-wide via AppTheme's android:textViewStyle. -->
    <style name="AppTheme.TextView" parent="Widget.AppCompat.TextView">
        <item name="android:shadowColor">@android:color/black</item>
        <item name="android:shadowRadius">3</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">0</item>
    </style>

</resources>
