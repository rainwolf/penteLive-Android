<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="be.submanifold.pentelive.BoardActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>
    <view
        class="be.submanifold.pentelive.BoardView"
        android:id="@+id/boardView"
        android:layout_below="@id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:id="@+id/submitLayout"
        android:layout_width="match_parent"
        android:orientation="horizontal"
        android:layout_below="@id/boardView"
        android:layout_height="wrap_content">
        <Button
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:drawableStart="@drawable/ic_skip_previous"
            android:textStyle="bold"
            android:id="@+id/backButton" />
        <Button
            android:layout_width="0dp"
            android:layout_weight="2"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:text="Submit"
            android:textStyle="bold"
            android:id="@+id/submitButton" />
        <Button
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:drawableEnd="@drawable/ic_skip_next"
            android:textStyle="bold"
            android:id="@+id/forwardButton" />
    </LinearLayout>
    <LinearLayout
        android:id="@+id/dPenteLayout"
        android:layout_width="match_parent"
        android:visibility="gone"
        android:orientation="horizontal"
        android:layout_below="@id/boardView"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_marginLeft="10dp"
            android:textSize="18dp"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:text="Play as:"/>
        <Button
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="white"
            android:focusable="false"
            android:textStyle="bold"
            android:id="@+id/playAsWhiteButton" />
        <Button
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:text="black"
            android:textStyle="bold"
            android:id="@+id/playAsBlackButton" />
    </LinearLayout>

    <com.google.android.gms.ads.AdView
        android:id="@+id/boardAdView"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_height="wrap_content"
        android:gravity="bottom|center"
        ads:adSize="BANNER"
        ads:adUnitId="@string/banner_ad_unit_id_board">
    </com.google.android.gms.ads.AdView>

    <TextView
        android:layout_width="match_parent"
        android:id="@+id/playerInfo"
        android:layout_marginLeft="5dp"
        android:gravity="fill_vertical|center"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="3dp"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"
        android:textSize="16dp"
        android:layout_height="fill_parent"
        android:background="@drawable/border"
        android:layout_below="@id/submitLayout"
        android:layout_above="@id/boardAdView"/>



</RelativeLayout>
